TODO:

	About:

		Linq style collection search tool
		collections must be array of: objects or dictionaries
		queries can be created statically or at runtime

# =================================================================
    Code taste:

	.where(
		CmprBuilder.and_({age=eq(4), name=eq("mike")}))
								CONVERT
	Where.new(
		CmprObj.And.new([CmprObj.new("age", cmps["age"]), CmprObj.new("name", cmps["name"])])

	.foreach(UpdFunc.new(funcref(self, "slct_func"), arg))
	.foreach(Upd.And.new([Upd.Add.new("age", 1), Upd.Set("mood", "bad"))])

	.where(Cmpr.And.new([CmprObj.Eq("age", 4), CmprObj.Eq("name", "mike")])

	class Cmpr:
		var field
		var val

		func _init(field, val):
			self.field = field
			self.val = val
			
		func eval(item):
			return true
		
	class Lt:
		extends Cmpr
		var field
		var val

		func _init(field, val).(field, val):
			pass
			
		func eval(item):
			return item[field] < val

# =================================================================
    improvements/planned features:		

		wrap predicate dict in object 

		naming
			Enumerator/ble
			Operators
				change to comparers?
				operators should change things
				OR Operators.Compare, Operators.Effect
				
		readme/docs
			github pages

		More relevant use of library in example
			multiple shops?
			search inventories, compare prices etc...

		### github ###

# =================================================================

		database, collection/list node
			get items on_enter
			editor gui
				view data; 
					filter - generate to target node??
				add?, remove?, edit?
		tests
			include gut? test branch?

		threaded/synchro search (stretch goal)
			could just port to c++?

		built in support for unique-id system
			is there a solution/function built in?
			
		iterators should limit possible functions
			i.e. no Count().Select()
			SelectIterator, EndQueryIterator

		query types (only on iterlist/editable):
			update_one, update_many, update_n
			add
			remove

		convert original example to gui + search options

		simple documentation with examples for each 
			like linq, mongodb docs

		functions
			change:
				append, remove -> return copy of original
				combine/union?
			generation (a la numpy):
				repeat(i) -> list of i repeated N times
			mathematical:
				sum, average, min, max

		search/select keys and values??

		json
			+ write support - not sure this is necessary

		better examples showing all major features

		monitoring/signals
			observer pattern
			on_destroy/create

		some method for accessing nested fields
			In builder method?:
				Start().First(cmps).Open("inventory").First({type=eq("sword")})


		actions (on data):
		for_each(action)
		mathematical functions
			Add, Sub, Mult, Div, Mod
		Append/Erase on signal/event?

		schema - some way to ensure inserted items fit template

	test library - gut??

	ecs impl example
		requires id system
		
Shopkeeper example
	UI:
		;::;
		®∫®¬g
		o/
		
	dialogue: (? separate example ?)
		search wares, gossip

	use more library features:
		filters, 
		sort